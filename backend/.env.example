# PENUMBRA backend configuration (copy to .env — never commit .env)

# Force live mode (pull real NOAA feeds). Default is Demo Mode (bundled data).
# PENUMBRA_LIVE=1

# Anthropic API key (optional; enables AI space-weather analyst briefings)
PENUMBRA_ANTHROPIC_API_KEY=
